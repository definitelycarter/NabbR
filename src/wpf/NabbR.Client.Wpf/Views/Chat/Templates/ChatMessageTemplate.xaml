<UserControl x:Class="NabbR.Views.Chat.Templates.ChatMessageTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:tmpl="clr-namespace:NabbR.Views.Chat.Templates"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <tmpl:UserTemplate Grid.Column="0" DataContext="{Binding Path=User}" />
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" 
                       TextWrapping="Wrap" 
                       Text="{Binding Path=Content}" />
            <ContentControl Grid.Row="1" Visibility="Collapsed" Content="{Binding}" />
        </Grid>
    </Grid>
</UserControl>
